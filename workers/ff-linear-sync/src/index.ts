/**
 * ff-linear-sync — Worker entry point
 *
 * Thin wrapper around @factory/linear-sync.
 * All route logic lives in packages/linear-sync/src/index.ts.
 */

import handler from '@factory/linear-sync'

export default handler
