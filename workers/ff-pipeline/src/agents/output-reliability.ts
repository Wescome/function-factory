/**
 * Output Reliability Agent version control marker.
 * Increment ORL_VERSION when introducing breaking changes
 * to the output reliability module to prevent inconsistencies.
 */
export const ORL_VERSION = 2;
