services:
  arangodb:
    image: arangodb:3.12
    ports:
      - "8529:8529"
    environment:
      ARANGO_ROOT_PASSWORD: factory-dev
    volumes:
      - arangodb_data:/var/lib/arangodb3
      - ./infra/arangodb/init:/docker-entrypoint-initdb.d

volumes:
  arangodb_data:
