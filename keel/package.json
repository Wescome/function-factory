{
  "name": "@koales/keel",
  "version": "0.1.0-freeze",
  "private": true,
  "type": "module",
  "description": "KEEL — frozen domain contracts (M1 / Phase 2). Ports, lineage contract, SE-Onto entities, pure loop policy. No substrate.",
  "scripts": {
    "lint:deps": "node scripts/lint-deps.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "gate": "npm run lint:deps && npm run typecheck && npm run test"
  },
  "devDependencies": {
    "typescript": "5.6.3",
    "vitest": "4.1.10",
    "@cloudflare/vitest-pool-workers": "0.18.0",
    "wrangler": "4.107.0",
    "@cloudflare/workers-types": "4.20250109.0"
  },
  "dependencies": {
    "agents": "0.17.3",
    "@cloudflare/codemode": "0.4.2"
  }
}