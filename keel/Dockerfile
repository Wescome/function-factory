# PLAYBOOK-KEEL-RUN-SUITE-001 (A3, Tier 4): the SandboxConnector's container
# image. Pinned to the exact @cloudflare/sandbox version this repo depends
# on (package.json) -- matches the published minimal template's own
# Dockerfile (github.com/cloudflare/sandbox-sdk/examples/minimal).
FROM docker.io/cloudflare/sandbox:0.12.4

EXPOSE 8080
