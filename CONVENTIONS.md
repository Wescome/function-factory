# Reversa

> Reverse Engineering framework installed in this project.

## How to use

To activate Reversa, type `reversa` alone in a message.

## Activation behavior

When the user enters `reversa` alone:

1. Activate the `reversa` skill available at `.agents/skills/reversa/SKILL.md`
2. Read the SKILL.md in full and follow Reversa instructions exactly

## Non-negotiable rule

Do not delete, modify, or overwrite pre-existing legacy project files.
Reversa writes **only** to `.reversa/` and `_reversa_sdd/`.


---

# Reversa

> Reverse Engineering framework installed in this project.

## How to use

To activate Reversa, type `reversa` alone in a message.

## Activation behavior

When the user enters `reversa` alone:

1. Activate the `reversa` skill available at `.agents/skills/reversa/SKILL.md`
2. Read the SKILL.md in full and follow Reversa instructions exactly

## Non-negotiable rule

Do not delete, modify, or overwrite pre-existing legacy project files.
Reversa writes **only** to `.reversa/` and `_reversa_sdd/`.
