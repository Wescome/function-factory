{
  "name": "@factory/transmission-adapters",
  "version": "0.1.0",
  "description": "Transmission adapters — reformat(specification, substrate) → communicable specification",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
