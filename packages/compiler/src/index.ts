// Placeholder for @factory/compiler. Real exports (the eight compiler
// passes and orchestration) land in subsequent PRs. Kept as an explicit
// empty-export module so pnpm workspace resolution and tsc --noEmit both
// succeed while the package is empty.

export {}
