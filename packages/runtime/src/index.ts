// Placeholder for @factory/runtime. Real exports land in the PR that
// implements this package. Kept as an explicit empty-export module so
// pnpm workspace resolution and tsc --noEmit both succeed while the
// package is empty.

export {}
