# Invariants

This directory is the reserved compatibility bucket for standalone `INV-*`
invariant artifacts.

Ontology v0.2 aliases this artifact family as `Invariant Specification`.
The physical path remains `specs/invariants/`, and invariant content may also
be embedded in IS- or Executable Specification-derived artifacts until a dedicated migration
separates those surfaces.
