# specs/deltas

This directory holds `DEL-*` artifacts produced by the Stage 4 capability-delta engine.

A delta artifact answers:
- which capability was evaluated
- whether that capability is missing, degraded, underutilized, or sufficient
- which dimensions were affected
- which evidence artifacts justified the judgment
- which Function types should be proposed downstream
